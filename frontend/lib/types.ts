export interface StudentData {
    id: string;
    name: string;
    email: string;
    dob: string;
    age: number;
}

export interface NewStudentData {
    name: string;
    email: string;
    dob: string;
}